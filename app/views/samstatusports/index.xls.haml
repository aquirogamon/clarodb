%table{:border => 1}
  %thead
    %tr
      %th.text-center
        Item
      %th.text-center
        Nodo
      %th.text-center
        Tipo de equipo
      %th.text-center
        Puerto
      %th.text-center
        Velocidad Configurada (Mbps)
      %th.text-center
        Tipo de puerto
      %th.text-center
        Descripción
  %tbody
    - unless Samstatusport.last == nil
      - i = 0
      - @table_statusport.map do |item|
        - i += 1
        %tr
          %td
            = i
          %td
            = item[:device]
          %td
            = item[:tipo_equipo]
          %td
            = item[:port]
          %td
            = item[:actualSpeed]
          %td
            = item[:speed]
          %td
            = item[:description]